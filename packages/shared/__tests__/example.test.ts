/**
 * Example test for shared package
 * This demonstrates Jest working with the monorepo shared package
 */

describe('Shared Package Example', () => {
  it('should run tests in shared package', () => {
    expect(1 + 1).toBe(2);
  });
});
